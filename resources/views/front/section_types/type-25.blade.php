<section class="careers accordions @background(stripslashes ($section->color))">
    <div class="row">
        @if($section->title)
            <h2>{{ $section->title }}</h2>
        @endif
    </div>
</section>
